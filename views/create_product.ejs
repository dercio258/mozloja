<!DOCTYPE html>
<html lang="pt">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MOZCOMPRAS - Criar Produto</title>
    <link rel="stylesheet" href="/css/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
</head>

<body>
    <nav>
        <div class="logo">MOZCOMPRAS</div>
        <div class="nav-links">
            <a href="<%= url('mydashboard') %>">Dashboard</a>
            <a href="<%= url('myvendas') %>">Vendas</a>
            <a href="<%= url('myproduto') %>">Produtos</a>
            <a href="<%= url('mysaques') %>">Saques</a>
            <a href="<%= url('mycriarproduto') %>">Cadastrar Produto</a>
            <a href="<%= url('myintegrations') %>">Integrações</a>
            <a href="/auth/logout">Sair</a>
        </div>
    </nav>

    <div class="container" style="max-width: 600px;">
        <h1>Cadastrar Novo Produto</h1>
        <form action="/products" method="POST">
            <div class="form-group">
                <label>Nome do Produto</label>
                <input type="text" name="name" placeholder="Ex: Curso de Marketing" required>
            </div>
            <div class="form-group">
                <label>Preço (MT)</label>
                <input type="number" name="price" placeholder="Ex: 1500" required>
            </div>
            <div class="form-group">
                <label>URL da Imagem</label>
                <input type="url" name="image" placeholder="https://exemplo.com/imagem.jpg" required>
            </div>
            <div class="form-group">
                <label>Descrição</label>
                <textarea name="description"
                    style="width: 100%; padding: 0.75rem; border: 1px solid var(--border-color); border-radius: 0.5rem; font-family: inherit;"
                    rows="4" placeholder="Breve descrição do produto..." required></textarea>
            </div>
            <div class="form-group">
                <label>Link do Conteúdo</label>
                <input type="url" name="content_link" placeholder="https://exemplo.com/curso" required>
            </div>

            <button type="submit" style="margin-top: 1rem;">Criar Produto</button>
        </form>
    </div>
</body>

</html>